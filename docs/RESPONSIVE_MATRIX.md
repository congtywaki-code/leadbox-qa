# Responsive Matrix

The default matrix covers 1440, 1280, 1024, 768, 430, 390 and 360 CSS-pixel widths. Each critical surface must pass containment, no-horizontal-overflow and screenshot comparison at every supported viewport. Device presets may be added by version profile, but removing a default viewport requires documented approval.
