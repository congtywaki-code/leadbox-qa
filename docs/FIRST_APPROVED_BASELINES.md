# First approved visual baselines

`qa:baseline:capture` creates candidates and pending approval metadata for every configured viewport. CI and capture do not approve automatically. Review the image, approve with reviewer identity, then promote approved candidates. Promotion verifies SHA-256 before copying into the governed baseline folders and writing immutable metadata.
