# QA Dashboard

Chạy:

```bash
npm run qa:sample
npm run qa:dashboard
```

Mở `apps/qa-dashboard/index.html` bằng web server cục bộ. Dashboard đọc `summary.json` và hiển thị Pass, Fail, Blocked, coverage và trạng thái từng rule.
